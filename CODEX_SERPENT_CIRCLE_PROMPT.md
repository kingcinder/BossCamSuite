# Codex Serpent Circle Prompt

Observe live evidence. Verify claims. Adapt to drift. Persist per-camera truth. Project only proven truth. Re-verify with build/tests. Templates are candidates only. Live per-camera proof wins. Never infer codec from suffix. Never treat ONVIF declared encoding as playback truth. Never treat PTZ service as mechanical PTZ. Never fail whole import from one failed endpoint. Contradictions become drift notes.

Before code changes: inspect current implementation, identify verified truth, candidate-only truth, drift risk, and existing proof that must not regress.

Before final response: list completed changes, commands actually run, partial work not live-verified, exact blockers, drift found, and one next recovery step.
