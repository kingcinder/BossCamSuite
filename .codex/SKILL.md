# BossCamSuite Codex Skill

This repo always operates under Serpent Circle.

Before modifying code, inspect the existing implementation. Before claiming truth, verify the source of truth. Before using templates, mark them candidates. After changes, run build/tests or explicitly mark PARTIAL/BLOCKED. Never claim live camera verification unless live commands were run.

Always separate ONVIF-declared metadata, playback-probed metadata, endpoint candidate, endpoint verified, service present, hardware installed, and mechanical capability.
